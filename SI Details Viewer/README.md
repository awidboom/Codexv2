# SI Details Viewer Handoff Package

This folder contains an offline facility audit quick-reference page built from a subject item workbook.

Run `python build_data.py`, then open `static/audit-reference.html`.
