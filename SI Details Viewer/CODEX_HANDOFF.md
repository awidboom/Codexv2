# Codex Handoff

Run `python build_data.py` and then open `static/audit-reference.html`.

`python build_baseline_data.py` is optional and will emit `static/baseline.js` as `null` when no baseline workbook is present.
